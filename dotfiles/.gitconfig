[user]
	name = Timon Ensel
	email = forgottosave.ensel@tum.de
[color]
	ui = auto
[filter "lfs"]
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
